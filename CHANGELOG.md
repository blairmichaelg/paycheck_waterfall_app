# Changelog

All notable changes to this repository will be documented in this file.

## [Unreleased]
- Add CI workflow to run tests, `black --check`, and `flake8` on PRs.
- Add `.pre-commit-config.yaml` with `black`, `flake8`, and basic hygiene hooks.
